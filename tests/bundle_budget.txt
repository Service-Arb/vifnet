# Maximum gzip bytes of first-load JavaScript on a place page
# (`/[locale]/[location]`), checked by `kitstart-size` after `next build`
# (and by `mkLanding`'s bundle-budget check on the Nix build).
#
# Measured on the first Vifnet page: 153,921 B gz (Next 16.3.6, Turbopack) —
# the kitstart template's 152,780 plus the before/after island and the
# service links. Set from that measurement plus ~2.5 %; raise it only in a
# commit that says why.
158000
